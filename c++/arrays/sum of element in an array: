//sum of element in an array:

#include <bits/stdc++.h> 
using namespace std; 
  
int main() 
{ int i,sum,n;
 
    int a[] = { 1, 4, 4, 1, 5, 2 }; 
    n= sizeof(a)/sizeof(a[0]);
  
  sum=0;
  for(i =0;i<n;i++)
   sum= sum+ a[i];
   cout<<"The sum of vector is "<< sum;
    return 0; 
} 
