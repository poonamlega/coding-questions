//sum of elements of a vector :

#include <bits/stdc++.h> 
using namespace std; 
  
int main() 
{ int i,sum;
    // Get the vector 
    vector<int> a = { 1, 4, 4, 1, 5, 2 }; 
  
  sum=0;
  for(i =0;i<a.size();i++)
   sum= sum+ a[i];
   cout<<"The sum of vector is "<< sum;
    return 0; 
} 
